import { User } from '@/types/types'
import React from 'react'

function ProjectCard({ user, activity }: { readonly user: User | undefined, readonly activity: string}) {
  return (
    <section>ProjectCard</section>
  )
}

export default ProjectCard