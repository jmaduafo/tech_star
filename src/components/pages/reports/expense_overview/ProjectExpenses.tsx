import { User } from '@/types/types'
import React from 'react'

function ProjectExpenses({ user }: { readonly user: User | undefined}) {
  return (
    <div>ProjectExpenses</div>
  )
}

export default ProjectExpenses