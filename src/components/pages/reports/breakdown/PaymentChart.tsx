import { User } from '@/types/types'
import React from 'react'

function PaymentChart({ user }: { readonly user: User | undefined}) {
    // TOTAL PAYMENT VS MONTH (LINE CHART)
  return (
    <div>PaymentChart</div>
  )
}

export default PaymentChart